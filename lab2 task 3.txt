#!/bin/bash
if [ -z "$1" ]
then
echo "Error: no argument"
exit 1
fi
catalog=$1
if [ -z "$2" ]
then
echo "Error: no argument"
exit 1
fi
roots=$2
if [ -z "$3" ]
then
echo "Error: no argument"
exit 1
fi
newroots=$3
find $catalog -perm $roots -exec chmod $newroots '{}' \;
